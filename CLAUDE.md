# Claude Instructions

You are working in the course content repository for Angewandte Systemwissenschaften 1.

Follow `AGENT.md` and `README.md`. The most important constraints are:

- Preserve material IDs and existing material file paths.
- Use `lesson_source_path` for lesson material attachments.
- Keep JSON syntactically valid and newline-terminated.
- Keep Markdown lessons under `lessons/` and exams under `tests/`.
- Never add secrets, credentials, grades, or private student data.

When asked to add course content, edit the repository files directly using the sync conventions. When asked to explain the repo, start with `README.md`.
